% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_bouquet.R
\name{summary.cluster_bouquet}
\alias{summary.cluster_bouquet}
\title{Summarise a cluster_bouquet result}
\usage{
\method{summary}{cluster_bouquet}(object, ...)
}
\arguments{
\item{object}{A \code{cluster_bouquet} object returned by \code{\link[=cluster_bouquet]{cluster_bouquet()}}.}

\item{...}{Unused; present for S3 compatibility.}
}
\value{
\code{object}, invisibly (so the result can still be piped).
}
\description{
Prints a structured report of the clustering: method, k selection scores
(when k was chosen automatically), silhouette quality, cluster sizes, and
per-series silhouette widths.
}
\examples{
set.seed(42)
n <- 52L
gw <- tibble::tibble(
  week    = rep(seq(as.Date("2023-01-01"), by = "week", length.out = n), 4L),
  station = rep(paste0("S", 1:4), each = n),
  level   = c(cumsum(rnorm(n)), cumsum(rnorm(n)),
              cumsum(rnorm(n)), cumsum(rnorm(n)))
)
summary(cluster_bouquet(gw, week, station, level))

}
