% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{save_bouquet}
\alias{save_bouquet}
\title{Save a bouquet plot to a file}
\usage{
save_bouquet(
  plot = ggplot2::last_plot(),
  file = "bouquet.png",
  width = 10,
  height = 8,
  units = "in",
  dpi = 300,
  ...
)
}
\arguments{
\item{plot}{A ggplot or patchwork object returned by \code{\link[=make_plot_bouquet]{make_plot_bouquet()}}.
Defaults to the last plot printed (\code{ggplot2::last_plot()}).}

\item{file}{Path to the output file. The format is inferred from the
extension (\code{.png}, \code{.pdf}, \code{.svg}, etc.). Default \code{"bouquet.png"}.}

\item{width}{Width in \code{units}. Default \code{10}.}

\item{height}{Height in \code{units}. Default \code{8}.}

\item{units}{One of \code{"in"}, \code{"cm"}, \code{"mm"}, or \code{"px"}. Default \code{"in"}.}

\item{dpi}{Resolution in dots per inch. Used for raster formats (PNG,
JPEG, TIFF). Default \code{300}.}

\item{...}{Additional arguments forwarded to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
The \code{file} path, invisibly.
}
\description{
A thin wrapper around \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} with sensible defaults for the
square-ish aspect ratio typical of bouquet plots, so you don't have to
remember the right \code{width}/\code{height} each time.
}
\examples{
\donttest{
set.seed(42)
n   <- 52L
gw  <- tibble::tibble(
  week    = rep(seq(as.Date("2023-01-01"), by = "week", length.out = n), 3L),
  station = rep(c("A", "B", "C"), each = n),
  level   = c(cumsum(rnorm(n)), cumsum(rnorm(n)), cumsum(rnorm(n)))
)
p <- make_plot_bouquet(gw, week, station, level)
tmp <- tempfile(fileext = ".png")
save_bouquet(p, tmp)
}

}
\seealso{
\code{\link[=make_plot_bouquet]{make_plot_bouquet()}} which produces the plots saved by this
function. \code{\link[=make_plot_bouquet_interactive]{make_plot_bouquet_interactive()}} for an interactive plotly
version that can be exported with \code{htmlwidgets::saveWidget()}.
}
