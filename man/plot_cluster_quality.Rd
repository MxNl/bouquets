% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_bouquet.R
\name{plot_cluster_quality}
\alias{plot_cluster_quality}
\title{Plot silhouette scores across k values for a cluster_bouquet result}
\usage{
plot_cluster_quality(x, dark_mode = FALSE)
}
\arguments{
\item{x}{A \code{cluster_bouquet} object returned by \code{\link[=cluster_bouquet]{cluster_bouquet()}}.}

\item{dark_mode}{Logical. Match the dark background of \code{\link[=make_plot_bouquet]{make_plot_bouquet()}}.
Default \code{FALSE}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object (invisibly), also printed to the device.
}
\description{
Draws a bar chart of the composite silhouette score (or plain mean
silhouette if \code{resolution = 0}) across all candidate k values evaluated
during automatic k selection. The selected k is highlighted. Use this to
inspect the elbow and decide whether a different k might be preferable.
}
\details{
This function requires that \code{\link[=cluster_bouquet]{cluster_bouquet()}} was called with
\code{k = "auto"} -- if a fixed k was supplied there are no scores to plot.
}
\examples{
\donttest{
set.seed(42)
n <- 52L
gw <- tibble::tibble(
  week    = rep(seq(as.Date("2023-01-01"), by = "week", length.out = n), 6L),
  station = rep(paste0("S", 1:6), each = n),
  level   = c(cumsum(rnorm(n)), cumsum(rnorm(n)), cumsum(rnorm(n)),
              cumsum(rnorm(n)), cumsum(rnorm(n)), cumsum(rnorm(n)))
)
result <- cluster_bouquet(gw, week, station, level)
plot_cluster_quality(result)
}

}
