% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{make_plot_bouquet_interactive}
\alias{make_plot_bouquet_interactive}
\title{Interactive Angular Accumulation Plot (plotly hover tooltips)}
\usage{
make_plot_bouquet_interactive(
  data,
  time_col = 1,
  series_col = 2,
  value_col = 3,
  stem_colors = "#3a7d2c",
  flower_colors = "#f472b6",
  ceiling_pct = 0.8,
  launch_deg = 90,
  dark_mode = FALSE,
  title = NULL,
  from = NULL,
  to = NULL,
  normalise = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{data}{A long-format data frame -- same format as \code{\link[=make_plot_bouquet]{make_plot_bouquet()}}.}

\item{time_col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Time column.
Default: first column.}

\item{series_col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Series
identifier column. Default: second column.}

\item{value_col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Numeric value
column. Default: third column.}

\item{stem_colors}{Single hex string or a named character vector (one hex
per series name). Default \code{"#3a7d2c"}.}

\item{flower_colors}{Single hex string or a named character vector for the
end-of-series markers. Default \code{"#f472b6"}.}

\item{ceiling_pct}{Fraction of maximum angle. Default \code{0.80}.}

\item{launch_deg}{Launch heading in degrees. Default \code{90}.}

\item{dark_mode}{Logical. Default \code{FALSE}.}

\item{title}{Plot title string. Default \code{NULL}.}

\item{from}{Optional time lower bound for filtering. Default \code{NULL}.}

\item{to}{Optional time upper bound for filtering. Default \code{NULL}.}

\item{normalise}{Logical. Per-series angle normalisation. Default \code{FALSE}.}

\item{verbose}{Logical. Print angle diagnostics. Default \code{FALSE}.}
}
\value{
A \code{plotly} htmlwidget object.
}
\description{
Builds the same turtle-graphics bouquet as \code{\link[=make_plot_bouquet]{make_plot_bouquet()}} and
converts it to an interactive plotly figure. Hovering over any point on a
path shows the series name, date/time, step number, and value.
}
\details{
This function requires the \pkg{plotly} package.
}
\examples{
\donttest{
if (requireNamespace("plotly", quietly = TRUE)) {
set.seed(42)
n   <- 52L
gw  <- tibble::tibble(
  week    = rep(seq(as.Date("2023-01-01"), by = "week", length.out = n), 3L),
  station = rep(c("A", "B", "C"), each = n),
  level   = c(cumsum(rnorm(n)), cumsum(rnorm(n)), cumsum(rnorm(n)))
)
make_plot_bouquet_interactive(gw, "week", "station", "level")
}
}

}
\seealso{
\code{\link[=make_plot_bouquet]{make_plot_bouquet()}} for the static ggplot2 version with full
styling options. \code{\link[=save_bouquet]{save_bouquet()}} to export the static version to a file.
\code{\link[=cluster_bouquet]{cluster_bouquet()}} to colour-code series by directional cluster before
plotting.
}
